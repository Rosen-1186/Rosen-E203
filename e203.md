# E203摸鱼日志

### 26/2/21
**当日事项**：
    1.配置完毕 e203_hbirdv2 仓库与 hbird_sdk（位于e203_hbirdv2/software目录内）;
    2.构建并运行 coremark 基准测试（ITER=1），获取基本性能水平；
    3.创建 github repo 并 push 到远程仓库。

**基本性能水平（运行时钟540999Hz，测试迭代20次）**：2.153600 CoreMark/MHz

**踩坑**：E203 架构的 ITCM 基地址为 0x80000000 ，用 DOWNLOAD=ilm 编译时无报错，但无法正常仿真（出现取指令死锁）。
**问题分析**：Verilog 的 Testbench 内用于模拟 ITCM 的数组长度可能过短，导致仿真器启动时出现越界报错，导致 ITCM 数组无法正常读取。
**解决方法**：在将 coremark.elf 转换成 coremark.verilog 时用 objcopy 平移机器码地址到 0x00000000 。
**使用指令**：
```bash
riscv64-unknown-elf-objcopy -O verilog --adjust-vma=-0x80000000 *filename*.elf *filename*.verilog
```
**次日安排**：
    1.审查 e203 的 Verilog RTL 仓库目录，搞懂顶层架构；
    2.基于 my_hello 创建波形测试架，基于 GTKWave 看波形变化，分析端口行为。

### 26/2/22
**当日事项**：摸鱼。

### 26/2/23
**当日事项**：收拾。

### 26/2/24
**当日事项**：赶路。

### 26/2/25
**当日事项**：赶路。

### 26/2/26
**当日事项**：聚。

### 26/2/27
**当日事项**：摸鱼。

### 26/2/28
**当日事项**：
    1.基于 GTKWave 查看并分析仿真波形，分析编译器和处理器在处理程序与机器码时的特性。

**踩坑 1**：
    在编译运行移位器的c代码时，为了查看波形搜索了机器码。手算的 32 位移位机器码是 00209093，然而 GTKWave 内检查时在 ifu_o_ir 检查时并未检出预期机器码。
**问题分析 1**：
    触发了 RISC-V 的 C 压缩指令集特性，将32位机器码 00209093 压缩为一个 16 位指令：008a。
**解决方法 1**：
    使用 objdump 反编译 .elf 文件得到 .dump 文件后查找有关主函数机器码，分析其与32位标准机器码的不同。
```dump
    800000b4 <main>:
    800000b4:	00100093          	li	ra,1
    800000b8:	008a                	slli	ra,ra,0x2
    800000ba:	9002                	ebreak
    800000bc:	4501                	li	a0,0
    800000be:	8082                	ret
```
**使用指令 1**：
```bash
riscv64-unknown-elf-objdump -d my_hello.elf > my_hello.dump
```

### 26/3/1
**当日事项**：摸鱼。

### 26/3/2
**当日事项**：补充架构文档，指定修改计划。

### 26/3/3
**当日事项**：
1.补齐 Phase0 计数器并重跑 CoreMark，形成可复现基线；
2.加入 stuck-PC 自动结束，保证 summary 稳定输出；
3.完成首版瓶颈归因采样并做 BHT 对比。

### 26/3/4
**当日事项**：
1.围绕 `DISP -> MDV` 瓶颈推进 M1，加入 MUL 实验路径（可开关）；
2.完成两组 A/B：`BHT on + MUL on` 与 `BHT off + MUL on`；
3.确认分支预测改动仍保留，并核对日志判读方式（`Leaving directory` 非失败信号）。

**进度同步（补充词条）**：
1.对 `disp_block_dep` 增加详细插针：`raw/waw`、`raw-only/waw-only/overlap`、`rs1/rs2/rs3` 命中、consumer 类型分布（ALU/AGU/BJP/CSR/MULDIV）；
2.重跑 CoreMark（BHT on + MUL on）并完成关键路径重排；
3.结论更新：`MDV` 已从主瓶颈降为次要项，当前主路径转为 `dep -> raw -> rs1`（集中在 ALU/BJP 消费端）。

**关键路径拆解（最新）**：
- `DISP dep = 44684`，其中 `raw = 44684`，`waw-only = 0`；
- `raw rs1-match = 43089`，`raw rs2-match = 1595`，`raw rs3-match = 0`；
- consumer 分布：`ALU=24708`，`BJP=14301`，`AGU=5675`，`CSR/MULDIV=0`。

**下一步（已定）**：
- 暂停新增探针，进入第一次功能改造 A/B；
- 优先目标：`ALU/BJP` 侧 `rs1` RAW 路径（保持宏开关可回退）。

**关键数据（基线，exp/phase0-counter-v2 @ bf14125）**：
- CoreMark/MHz（ITER=1）：**2.154100**
- IPC：**0.600**（`406748 / 678474`）
- Branch miss rate：**16.25%**（`21191 / 130419`）
- Top1 瓶颈：**rsp->IR/EXU 背压主导**（`ifu_rsp_block/non_retire=86.99%`，`ir_busy/non_retire=85.10%`）

**实验汇总表（统一口径，仅保留一张）**：

| 日期 | 分支/Commit | 配置摘要 | CoreMark/MHz | IPC | branch_miss_rate | MDV block MUL cycles | ifu_flush/non_retire | ifu_bpu_wait/non_retire | lsu_wait/non_retire | ifu_req_block/non_retire | ifu_rsp_block/non_retire | ir_busy/non_retire | 结论 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 26/3/3 | exp/phase0-counter-v2 / bf14125 | Phase0计数器+stuck-PC自动结束，BHT关闭 | 2.154100 | 0.600 | 16.25% | - | 8.23% | 0.86% | 1.82% | 0.00% | 86.99% | 85.10% | rsp->IR/EXU 背压主导 |
| 26/3/3 | (BHT on) b3cce04 | Phase1(BHT)+同口径计数器 | 2.177890 | 0.607 | 10.22% | 160288 | 5.41% | 0.89% | 1.87% | 0.00% | 89.64% | 87.72% | 分支代价下降，但 rsp/IR 背压仍主导 |
| 26/3/4 | (BHT on + MUL on) | Phase1(BHT) + M1(MUL实验路径开启) | 3.230183 | 0.797 | 10.19% | 772 | 13.69% | 2.25% | 4.76% | 0.00% | 73.76% | 68.93% | MUL窗口显著缩短，整体吞吐大幅提升 |
| 26/3/4 | (BHT off + MUL on) | 关闭BHT，保留M1(MUL实验路径) | 3.178124 | 0.784 | 16.27% | 772 | 19.96% | 2.08% | 4.40% | 0.00% | 68.44% | 63.87% | 在MUL优化基础上，BHT仍贡献约1~2%增益 |

> 口径说明：`xxx/non_retire` 的分母统一为 `NonRetireCycle = cycle_count - valid_inst`；各项可能重叠，百分比之和不要求等于 100%。
> 说明：`ITER=1` 仅用于快速 A/B 和路径归因，不是 CoreMark 规范成绩（官方要求运行时间 >=10s）。
